profiles:
  development:
    base_url: http://nick-harvey.github.io/
    deploy:
      host: github_pages
      # use gh-branch for repositories other than <username>.github.io
      branch: gh-pages
      # use master branch for <username>.github.io repository
      branch: master
  production:
    base_url: http://www.openshift.org/
    deploy:
      host: ww2-originsites.rhcloud.com
      path: /var/lib/openshift/5515bf4b4382ec090d00007c/app-root/runtime/repo/php
